reference: https://zetcode.com/springboot/resttemplate/


-> create folder 
    -> open visual studio code
        -> open terminal 
            -> go to the folder using cd on terminal
                -> type npm init 

    the project name should be start with Uppercase and 
    should be the same name with module that wil be used 
    also notice the entry point on package.json which is 
    the manually created generate_fake_users.js file

                    -> type npm i json-server
                    -> type npm i faker fs

    manually create generate_fake_users.js file and write the code

                    -> type node generate_fake_users.js
    
    data.json file will be created on the same directory 
    which will contain 100 users generated by faker fs

                    -> type npx json-server --watch data.json

    the server will be up and running
  
            \{^_^}/ hi!      

            Loading data.json
            Done

            Resources
            http://localhost:3000/users

            Home
            http://localhost:3000

            Type s + enter at any time to create a snapshot of the database
            Watching...




